name: Changesets

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write
  packages: write

jobs:
  version_or_publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://npm.pkg.github.com
          scope: "@stoney-dev"

      - uses: pnpm/action-setup@v4

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Create Version Packages PR OR publish after merge
        uses: changesets/action@v1
        with:
          version: pnpm changeset version
          publish: pnpm -C packages/runner publish --no-git-checks
        env:
          # PAT for PR creation (because repo blocks default GITHUB_TOKEN from creating PRs)
          GITHUB_TOKEN: ${{ secrets.CHANGESETS_TOKEN }}

          # GitHub Packages publish auth
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}